# Application Configuration
NODE_ENV=development
API_KEY=dev-api-key-123
HOST_PORT=3030
ENABLE_LOGGER=true
APP_TIMEZONE=Asia/Jakarta

# Security Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3030

# Webhook Configuration
WEBHOOK_URL=https://example.com/webhook
WEBHOOK_EXCLUDE_ME=false
WEBHOOK_EXCLUDE_GROUP=false
WEBHOOK_EXCLUDE_CHANNEL=false

# Rate Limiting
RATE_LIMIT=100
RATE_LIMIT_EXPIRE=60

# Message Configuration
MESSAGE_DELAY_TYPE=fixed
MESSAGE_DELAY=1

# Upstash Redis Configuration (using placeholder values for testing)
UPSTASH_REDIS_REST_URL=placeholder-url
UPSTASH_REDIS_REST_TOKEN=placeholder-token

# Upstash Workflow Configuration (QStash) (using placeholder values for testing)
QSTASH_URL=https://qstash.upstash.io/v2
QSTASH_TOKEN=placeholder-token
QSTASH_CURRENT_SIGNING_KEY=placeholder-key
QSTASH_NEXT_SIGNING_KEY=placeholder-key
WORKFLOW_BASE_URL=http://localhost:3030